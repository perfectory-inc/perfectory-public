listen_addresses: 0.0.0.0:3000
on_invalid: abort
pmtiles:
  # Local proof accepts a loopback/public test URL. Production must use HTTPS and a read-only
  # R2-backed `pmtiles.paths` configuration; this named source is intentionally proof-only.
  allow_http: true
  sources:
    foundation_static: ${TILES_SLICE_PMTILES_URL}
