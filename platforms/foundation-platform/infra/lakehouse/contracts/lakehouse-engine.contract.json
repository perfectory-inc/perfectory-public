{
  "schema_version": 2,
  "purpose": "Single source of truth for the packages every lakehouse Spark job submits with. The Iceberg version lived in twelve files before this one existed, so raising it meant editing twelve places and nobody ever did: the deployment sat on 1.6.1 while five releases went by.",
  "iceberg": {
    "version": "1.11.0",
    "artifacts": [
      "org.apache.iceberg:iceberg-spark-runtime-3.5_2.12",
      "org.apache.iceberg:iceberg-aws-bundle"
    ],
    "minimum_version": "1.8.0",
    "minimum_version_reason": "Before 1.8.0 the vectorized Parquet reader corrupts native memory: apache/iceberg#12178, 'Incorrect Results and SIGSEGV on Read', fixed in 1.8.0. Measured on this deployment 2026-08-29 against silver.building_register_units with vectorization on and nothing else changed: 1.6.1 died with SIGSEGV, 1.11.0 read the table. The three tables above ten million rows all died; the two in the low thousands did not, which is why it stayed hidden until a compaction run touched a large table. Root ADR-0064.",
    "scope": "Iceberg's own storage layer. iceberg-aws-bundle backs S3FileIO, which is how Iceberg reads and writes the files of its tables. It is not the filesystem Spark reads a plain file through, which is what the hadoop block below is for."
  },
  "hadoop": {
    "version": "3.3.4",
    "artifacts": [
      "org.apache.hadoop:hadoop-aws"
    ],
    "version_reason": "Must equal the Hadoop the Spark image already carries. Measured 2026-08-31 by listing /opt/spark/jars in apache/spark:3.5.6-scala2.12-java17-python3-ubuntu: hadoop-client-api-3.3.4.jar and hadoop-client-runtime-3.3.4.jar. A different hadoop-aws brings a second copy of classes the runtime already defines.",
    "scope": "The `s3a://` filesystem, so `spark.read` can open an object that is not part of an Iceberg table — the Silver handoff JSONL a load consumes. That image ships no aws, s3, or hadoop-cloud jar at all: 252 jars, none of them. Without this, `spark.read.json(\"s3a://...\")` fails on the scheme while the same job writes Iceberg tables to the same bucket, because those two go through different code."
  },
  "spark": {
    "version": "3.5.6",
    "scala_binary_version": "2.12",
    "artifact_suffix_reason": "The Iceberg runtime artifact name embeds both, so the artifacts above are only valid for this pair."
  }
}
